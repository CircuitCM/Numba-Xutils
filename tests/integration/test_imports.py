from __future__ import annotations

import nbux
from nbux import rng, utils, vector


def main() -> None:
    assert nbux is not None
    assert rng is not None
    assert utils is not None
    assert vector is not None


if __name__ == "__main__":
    main()
